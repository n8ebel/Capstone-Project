<resources>

    <!-- Base application theme. -->
    <style name="BaseAppTheme" parent="Theme.AppCompat.Light.NoActionBar">

        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

    </style>

    <style name="AppTheme" parent="BaseAppTheme">

    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="AppBarLayout">
        <item name="android:theme">@style/AppTheme.AppBarOverlay</item>
        <item name="popupTheme">@style/AppTheme.PopupOverlay</item>
    </style>

    <style name="BaseAppTheme.AddAccount.TextInputLayout" parent="BaseAppTheme">
        <item name="colorControlNormal">@android:color/white</item>
        <item name="colorControlActivated">@android:color/white</item>
        <item name="colorControlHighlight">@android:color/white</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="android:textColorSecondary">@android:color/holo_green_dark</item>
    </style>

    <style name="BaseAppTheme.AddAccount.Button" parent="BaseAppTheme">
        <item name="colorAccent">@color/primary</item>
        <item name="android:textColor">@android:color/white</item>
    </style>

    <style name="foo" parent="@style/TextAppearance.AppCompat">
        <item name="android:textColorHint">@color/primary</item>
        <item name="android:textColor">@color/primaryLight</item>
    </style>

</resources>
